diff --git a/ui/base/BUILD.gn b/ui/base/BUILD.gn
index 1388140fcb81fcd56404aba6dea3ec32911b963c..61fdaa7fe4dbd6b6ec5927052ac2c1b962fad972 100644
--- a/ui/base/BUILD.gn
+++ b/ui/base/BUILD.gn
@@ -419,7 +419,7 @@ component("base") {
       "themed_vector_icon.h",
     ]
 
-    if (!is_android && !is_ios) {
+    if (true || !is_android && !is_ios) {
       sources += [
         "menu_source_utils.cc",
         "menu_source_utils.h",
@@ -643,7 +643,7 @@ component("base") {
     ]
   }
 
-  if (!toolkit_views && !use_aura) {
+  if (!is_android && !toolkit_views && !use_aura) {
     sources -= [
       "dragdrop/drop_target_event.cc",
       "dragdrop/drop_target_event.h",
@@ -660,11 +660,17 @@ component("base") {
     deps += [ ":ui_base_jni_headers" ]
     libs += [ "jnigraphics" ]
 
+    if (false) {
     sources -= [
       "l10n/l10n_font_util.cc",
       "models/button_menu_item_model.cc",
       "theme_provider.cc",
     ]
+    }
+    sources += [
+      "dragdrop/os_exchange_data_provider_non_backed.cc",
+      "dragdrop/os_exchange_data_provider_non_backed.h",
+    ]
   }
 
   if (is_android || is_ios) {
